import Navbar from "./Navbar";
import NotificationBar from "./notificationBar";

const Header = () => {
  return (
    <header>
      <NotificationBar />
      <Navbar />
    </header>
  );
};

export default Header;
